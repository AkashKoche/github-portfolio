apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-portfolio.fullname" . }}
  labels:
    {{- include "github-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "github-portfolio.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
