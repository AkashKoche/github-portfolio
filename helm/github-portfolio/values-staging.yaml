replicaCount: 1

image:
  pullPolicy: Always
  tag: "latest"

autoscaling:
  enabled: false

resources:
  requests:
    memory: "32Mi"
    cpu: "50m"
  limits:
    memory: "64Mi"
    cpu: "100m"

config:
  appName: "GitHub-Portfolio (Staging)"
  cacheDuration: "60"
